<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="Theme.NeoKotlinUIConverted" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Standard AppCompat color attributes -->
    <item name="colorPrimary">@color/purple_500</item>
    <item name="colorPrimaryDark">@color/purple_700</item> <!-- Typically used for status bar with AppCompat -->
    <item name="colorAccent">@color/teal_200</item>      <!-- Standard accent color for AppCompat -->

    <!-- Text colors (optional, but good to define) -->
    <!-- <item name="android:textColorPrimary">@color/some_primary_text_color</item> -->

    <!-- Status bar color, using the standard AppCompat attribute -->
    <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryDark</item>
  </style>
</resources>